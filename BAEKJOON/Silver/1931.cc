#include <iostream>
#include <vector>
#include <set>

int main() {
    int N;
    std::vector<std::pair<int, int>> vec;
    int start, end;
    for(int i = 0; i < N; i++) {
        std::cin >> start >> end;
        vec.push_back({start, end});
    }


}