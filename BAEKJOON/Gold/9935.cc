#include <iostream>
#include <string>
#include <stack>

int main() {
    std::string str, boom;
    std::cin >> str >> boom;

    std::stack<char> s;

    while(true) {
        for(char c : str) {
            
        }
    }
}