#include <iostream>

int main() {
    int N,M;
    std::cin >> N;
    M = N;
    while(true) {
        int front;
        int end;
        if(M < 10)
            front = 0;
        
        else {front = M / 10;}

    }

}